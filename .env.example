APIFY_TOKEN=

# Telegram notifications (cron only — leave blank locally)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Vision Board integration (Supabase). Find values in Vision Board's .env.local.
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
# Optional overrides — defaults bake the existing auth uid + 'Rental' category id
# VISION_BOARD_OWNER_ID=
# VISION_BOARD_CATEGORY_ID=
